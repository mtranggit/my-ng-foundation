import { NgModule } from '@angular/core';
import { FlexboxLayoutRoutingModule, routedComponents } from './flexbox-layout-routing.module';

@NgModule({
    imports: [FlexboxLayoutRoutingModule],
    declarations: routedComponents
})
export class FlexboxLayoutModule {}
